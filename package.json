{
  "name": "sthira-ads-public-control",
  "private": true,
  "scripts": {
    "test": "node --test",
    "verify": "node --test && node -e \"JSON.parse(require('fs').readFileSync('config/active.json')); JSON.parse(require('fs').readFileSync('config/schema.json'))\""
  }
}
